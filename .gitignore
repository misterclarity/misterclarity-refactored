Thumbs.db
.DS_Store
_site
.jekyll-cache
.jekyll-metadata
.dist
.tmp
.sass-cache
npm-debug.log
node_modules
builds
package-lock.json
.scss
.css.map

# Local-only tooling. The resume generator carries the master profile and points at a
# tailnet-only LLM, and this repo has to stay public for GitHub Pages to serve the site.
tools/